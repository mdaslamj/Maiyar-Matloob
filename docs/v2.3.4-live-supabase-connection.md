# Version 2.3.4 — Live Supabase Connection & End-to-End Verification

**Status:** Phase 1 blocked — migrations not yet applied on Supabase project  
**Objective:** Replace sample dashboard data with real community data using the existing Supabase provider.

---

## Current Evidence (2026-07-04)

Connection probe against your project:

| Check | Result | Evidence |
|-------|--------|----------|
| Project URL reachable | PASS | `https://cfbdzxmobbnihdqtetdu.supabase.co` responds |
| Publishable key accepted | PASS | PostgREST responds (not 401) |
| Table `public.participants` | **FAIL** | `PGRST205: Could not find the table 'public.participants' in the schema cache` |
| Migrations 001–004 applied | **NOT YET** | Requires SQL Editor (Option A) |

**Phase 1 is not complete until you run the SQL below.**

---

## Phase 1 — Connect (Your Action Required)

### Step 1 — Local configuration (done in repo)

| Item | Location | Status |
|------|----------|--------|
| Supabase URL + publishable key | `src/backend/config/backend-config.local.js` | Configured (gitignored) |
| Backend sync enabled | `src/shared/feature-flags.js` | `USE_BACKEND_SYNC: true`, `BACKEND_PROVIDER: "supabase"` |
| `.gitignore` | root | `backend-config.local.js` excluded from git |

Credentials are **not** hardcoded outside `backend-config.local.js`.

### Step 2 — Apply migrations (Option A — Recommended)

1. Open [Supabase Dashboard](https://supabase.com/dashboard) → your project
2. Go to **SQL Editor**
3. Open `supabase/apply-all-migrations.sql` from this repo
4. Paste the **entire file** and click **Run**
5. Confirm success (no errors)

This applies migrations **001 → 004** plus API grants for the publishable key.

### Step 3 — Verify Phase 1

```powershell
node scripts/supabase-live-verification.mjs --phase 1
```

**Expected:** all tables PASS, all RPCs PASS.

---

## Phase 2 — Write Test

After Phase 1 passes:

```powershell
node scripts/supabase-live-verification.mjs --phase 2
```

**Expected evidence:**

- Script prints participant and assessment rows from REST API
- Supabase Table Editor shows rows in `participants`, `reports`, `assessments`

**Manual browser test (recommended in addition):**

1. Serve the app locally (any static server from project root)
2. Complete one real assessment in the student app
3. Confirm the same three tables receive rows

---

## Phase 3 — Dashboard

Admin access with Supabase uses **Storage Verification Mode** (authentication still deferred to v2.4):

- Banner: *Storage Verification Mode — Authentication Disabled*
- Topbar badge: **Live Data** (when sync enabled)
- Four dashboard widgets load from Supabase via existing backend-service loaders

```powershell
node scripts/supabase-live-verification.mjs --phase 3
```

Then open `/admin` in the browser and capture a dashboard screenshot.

**Expected:** Total Participants, Completed Assessments, Latest Assessment, and Average Implementation show live values — not sample placeholders.

---

## Phase 4 — Validation

```powershell
node scripts/supabase-live-verification.mjs --phase 4
```

Or run everything:

```powershell
node scripts/supabase-live-verification.mjs --all
```

**Validates:**

| Widget | Rule |
|--------|------|
| Total Participants | Increases when new participants submit |
| Completed Assessments | Increments per submission |
| Latest Assessment | Updates to most recent timestamp |
| Average Implementation | Mean of each participant's **latest** score only (Domain rule) |

---

## Evidence Checklist (Required Before Declaring Complete)

For each phase, capture:

- [ ] **Screenshot:** Supabase Table Editor (`participants`, `reports`, `assessments`)
- [ ] **Screenshot:** Admin dashboard with **Live Data** badge and widget values
- [ ] **Test description:** exact command or user flow performed
- [ ] **Script output:** `supabase-live-verification.mjs` summary with 0 FAIL

Do **not** mark v2.3.4 complete without all four.

---

## Files Added / Modified (v2.3.4 setup)

| File | Purpose |
|------|---------|
| `supabase/apply-all-migrations.sql` | Option A — single paste for SQL Editor |
| `src/backend/config/backend-config.local.js` | Local credentials (gitignored) |
| `src/shared/feature-flags.js` | Enable Supabase sync |
| `scripts/supabase-live-verification.mjs` | Phased automated verification |
| `src/admin/admin-auth-gate.js` | Storage verification admin access |
| `src/admin/admin-app.js` | Live dashboard bootstrap for Supabase |
| `src/admin/admin-layout.js` | Live Data badge + verification banner |
| `src/admin/admin-navigation.js` | Live data sidebar note |
| `src/admin/admin-dashboard.js` | Live vs sample footer text |

---

## What Is NOT Done Yet

- Migrations not applied on Supabase (blocked on SQL Editor step)
- No confirmed rows in production database
- No dashboard screenshot with live submission data
- Phases 2–4 not verified

---

## Next Step

**Run `supabase/apply-all-migrations.sql` in the Supabase SQL Editor**, then reply with:

1. Confirmation SQL ran successfully, or paste any error message
2. Output of `node scripts/supabase-live-verification.mjs --phase 1`

After Phase 1 passes, we proceed to Phases 2–4 with full evidence.
